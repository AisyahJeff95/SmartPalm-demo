# Placeholder for future Page 2 (Ready to overwrite)
from PySide6.QtWidgets import QWidget, QVBoxLayout, QLabel

class Page2(QWidget):
    def __init__(self):
        super().__init__()
        layout = QVBoxLayout(self)
        layout.addWidget(QLabel("Page 2 Placeholder"))
